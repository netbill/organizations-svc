type: object
required:
  - data
  - links
properties:
  data:
    type: array
    items:
      $ref: './InviteData.yaml'
  included:
    $ref: './InviteIncluded.yaml'
  links:
    $ref: './PaginationData.yaml'