type: object
required:
  - data
  - links
properties:
  data:
    type: array
    items:
      $ref: './OrganizationData.yaml'
  included:
    type: array
    items:
      oneOf:
        - $ref: './MemberData.yaml'
  links:
    $ref: './PaginationData.yaml'