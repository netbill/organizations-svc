type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - type
      - attributes
    properties:
      type:
        type: string
        enum: [ organization ]
      attributes:
        type: object
        required:
          - name
        properties:
          name:
            type: string
            description: "The name of the organization"
