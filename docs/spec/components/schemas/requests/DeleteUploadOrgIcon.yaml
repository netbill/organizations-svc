type: object
required:
  - data
properties:
  data:
    type: object
    required:
      - id
      - type
      - attributes
    properties:
      id:
        type: string
        format: uuid
        description: "organization id"
      type:
        type: string
        enum: [ delete_organization_upload_icon ]
      attributes:
        type: object
        required:
          - icon_key
        properties:
          icon_key:
            type: string
            description: "icon media key"
