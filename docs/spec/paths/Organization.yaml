parameters:
  - in: path
    name: organization_id
    required: true
    schema:
      type: string
      format: uuid
    description: Organization ID

get:
  tags:
    - organizations
  summary: Get organization
  description: >
    Returns an organization by ID.

    **400 Bad Request** is returned when the organization ID is not a valid UUID.
    **404 Not Found** is returned when the organization does not exist.
    **500 Internal Server Error** is returned when an unexpected error occurs.
  responses:
    '200':
      description: Organization successfully retrieved
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Organization.yaml'
    '400':
      description: >
        Bad Request. Provided organization ID is not a valid UUID.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'
    '404':
      description: >
        Not Found. Organization does not exist.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'
    '500':
      description: Internal Server Error
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'

patch:
  tags:
    - organizations
  summary: Update organization
  description: >
    Updates an organization by ID.

    **400 Bad Request** is returned when the organization ID is not a valid UUID or request body is invalid.
    **401 Unauthorized** is returned when the authorization token is missing or invalid.
    **500 Internal Server Error** is returned when an unexpected error occurs.
  security:
    - BearerAuth: [ ]
  requestBody:
    required: true
    content:
      application/json:
        schema:
          $ref: '../components/schemas/requests/UpdateOrganization.yaml'
  responses:
    '200':
      description: Organization successfully updated
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Organization.yaml'
    '400':
      description: >
        Bad Request. Provided organization ID is not a valid UUID or request body is invalid.
        Check the `errors` array for details.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'
    '401':
      description: >
        Unauthorized. Missing or invalid authorization token.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'
    '500':
      description: Internal Server Error
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'

delete:
  tags:
    - organizations
  summary: Delete organization
  description: >
    Deletes an organization by ID.

    **400 Bad Request** is returned when the organization ID is not a valid UUID.
    **401 Unauthorized** is returned when the authorization token is missing or invalid.
    **403 Forbidden** is returned when the organization is suspended, the initiator is not a member,
    does not have enough rights, or the organization has an active place.
    **404 Not Found** is returned when the organization does not exist.
    **500 Internal Server Error** is returned when an unexpected error occurs.
  security:
    - BearerAuth: [ ]
  responses:
    '204':
      description: Organization successfully deleted
    '400':
      description: >
        Bad Request. Provided organization ID is not a valid UUID.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'
    '401':
      description: >
        Unauthorized. Missing or invalid authorization token.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'
    '403':
      description: >
        Forbidden. Organization is suspended, initiator is not a member of the organization,
        does not have enough rights to delete, or organization has an active place.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'
    '404':
      description: >
        Not Found. Organization does not exist.
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'
    '500':
      description: Internal Server Error
      content:
        application/json:
          schema:
            $ref: '../components/schemas/responses/Errors.yaml'